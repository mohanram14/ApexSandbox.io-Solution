public String nameFromEmail(String email) {
    string name = email.substringbetween(0, indexofchar(.));
    name = name + ' ' + email.substringbetween(indexofchar(.), insedofchar(@));
    return name;
}