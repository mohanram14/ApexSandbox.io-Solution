public String nameFromEmail(String email) {
    string name = email.substringBefore('.').toLowerCase().capitalize();
    name = name + ' ' + email.substringbetween('.', '@').toLowerCase().capitalize();
    return name;
}