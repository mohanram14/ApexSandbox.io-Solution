public List<Account> getAccountsFromJSONString(String inputJSON){
    if(inputJSON == null || String.isBlank(inputJSON))  return null;
    List<Account> a = (List<Account>) JSON.deserialize(inputJSON, List<Account>.class);

    system.debug(a);
    return a;
}