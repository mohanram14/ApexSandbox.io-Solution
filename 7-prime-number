public Boolean isPrime(Integer num) {
    if(num < 2) return false;
    if(num == 2 || nums == 3) return true;
    for(Integer i = 0; i <= (nums/2); i++ ) {
        if(math.mod(nums, i) == o) return false;
    }
    return true;
}